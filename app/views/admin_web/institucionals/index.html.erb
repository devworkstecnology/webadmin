<div class="row">
  <div class="col-md-8">
    <h4>Cadastro de institucionais</h4>

    <div class="row">
      <div class="col-md-12"><%= link_to 'Nova', new_institucional_path, class: 'btn btn-primary link-new' %></div>
    </div>

    <div class="table-responsive">
      <table class="table table-hover">
        <thead>
          <tr>
            <td>Título</td>
            <td>Criado em</td>
          </tr>
        </thead>

        <tbody>
          <% @institucionals.each do |institucional| %>
            <td><%= link_to institucional.name, edit_institucional_path(institucional) %></td>
            <td><%= I18n.l institucional.created_at, format: :only_date %></td>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>

  <div class="col-md-4 box">
    <h4>Pesquisa</h4>
  </div>
</div>
